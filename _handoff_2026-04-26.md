# Handoff — 2026-04-26 — 8-Agent Team (Self-Contained Architecture)

## What we did

Completed a full overhaul to make the repo a self-contained prompt template system:

1. **Removed external MCP dependencies** (`350aaba`)
   - Removed `brain-router` and `engram` MCP server blocks from `opencode.json`
   - Replaced MCP tool references in all prompts with file-based memory protocol
   - Memory now reads/writes to `~/.opencode/projects/<project>/memory/`
   - Zero external dependencies required to use the prompt system

2. **Restored orchestrator depth** (`350aaba`)
   - Restored from 78 → ~220 lines
   - Added back: Delegation Gate, Anti-Pattern Guard, STOP Rule, Broad Review Rule,
     Route-Level Fast/Slow Ownership, Oscillation Control, Memory Retrieval Protocol,
     Budget Gate, Delegation Packet Metadata, Intent Lock, Clear-Scope Implementation
     Routing, Implementation Ownership Guard, Escalation Protocol
   - Validation: 0 errors (was 32)

3. **Scrubbed AI slop** (`ae5fb7c`)
   - `agents/explorer.md`: Replaced "immortal wanderer... cursed with eternal curiosity"
     with "systematic codebase mapper"
   - `agents/auditor.md`: Replaced "last of a lineage of builders... foundations of
     the digital world" with "disciplined builder who prefers verified facts"
   - Principle established: prompts describe what agents DO, not who they ARE

4. **Fixed AGENTS.md**
   - Removed irrelevant "KING MODE frontend architect" persona
   - Replaced with proper 8-agent team instructions

5. **Updated validator scripts**
   - `scripts/validate-agents.js`: Removed mandatory MCP checks, allows council model overrides
   - `scripts/validate-reasoning-scenarios.js`: Removed memory plugin checks

## Why we did it

The repo was graded D+ due to:
- External MCP dependencies making it seem like a runtime product
- Orchestrator stripped 92% without documented reasoning (causing 32 validation errors)
- AI slop in prompts
- Irrelevant AGENTS.md
- Hardcoded API key (even free-tier, still bad hygiene)

## How we did it

- Rewrote `_shared/memory-systems.md` with file-based paths
- Rewrote `_shared/cognitive-kernel.md` removing all MCP tool names
- Expanded `agents/orchestrator.md` with full routing guardrails
- Updated validator regexes to match new self-contained architecture
- Fixed `examples/standard.json` to match `opencode.json` exactly

## What's left to do

- **Add tests** — `tests/` has one markdown fixture. Need tests for `compose-prompts.js`
  (frontmatter parsing, shared block injection, incremental rebuild) and `validate-agents.js`
- **Scrub remaining AI slop** — Check other agent prompts for purple prose
- **Update README** — Still claims "22-step decision tree" which oversells the routing table
- **Free model fragility** — Council agents use free OpenRouter models
  (`gpt-oss-120b:free`, `mimo-v2-flash:free`, `qwen3-235b-a22b-thinking-2507:free`)
  that may break when models change. Consider pinning or documenting fallback.

## Version

- `opencode.json`: no version field (inherits from OpenCode)
- `examples/standard.json`: `1.8.0`
- Git: `ae5fb7c` on `origin/main`

## Validation Status

- 0 errors, 1 warning (placeholder API key)

## Files Changed (17 files across 3 commits)

- `AGENTS.md`, `README.md`, `opencode.json`, `examples/standard.json`
- `_shared/cognitive-kernel.md`, `_shared/memory-systems.md`
- `agents/orchestrator.md`, `agents/explorer.md`, `agents/auditor.md`
- `scripts/validate-agents.js`, `scripts/validate-reasoning-scenarios.js`
- Generated prompts regenerated
