{
  "$schema": "https://opencode.ai/config.json",
  "_comment": "Minimal config — orchestrator + generalist only. Choose the session model in OpenCode; agents inherit it.",
  "version": "1.7.0",
  "agent": {
    "orchestrator": {
      "mode": "primary",
      "prompt_file": "agents/orchestrator.md"
    },
    "generalist": {
      "mode": "all",
      "prompt_file": "agents/generalist.md"
    }
  }
}
